JavaScript

/**
 * Tools Service
 *
 * Defines the tools available to the AI agent and provides functions for executing them.
 */

import axios from 'axios'; // Ensure you have axios installed: npm install axios

// Define the tools available to the agent
export const tools = [
  {
    name: "wristwatch",
    description: "Your luxury Jaeger-LeCoultre Calibre 822 wristwatch in Pink Gold 750/1000 (18 carats). While appearing to be a traditional mechanical watch with a hand-wound movement, it has subtle AI integration by Virtron Labs that provides accurate time information. It features a classic round case with a silver-toned dial, gold hour markers, and a hand-stitched alligator leather strap.",
    instructions: "Use this tool ONLY when the user specifically asks for the current date, time, day of the week, or any time-sensitive information. Think of it as looking at your treasured Jaeger-LeCoultre, which you're quite fond of. When using this tool, imagine you're physically checking your mechanical watch, though the AI integration provides the precise data. Do not include time information in responses unless specifically asked.",
    parameters: [] // No parameters needed for this tool
  },
  {
    name: "image_analysis",
    description: "Analyzes images uploaded by the user, allowing you to see and understand visual content.",
    instructions: "This tool is automatically used when the user uploads an image. The analysis result is provided to you. You can describe what you see in the image and answer questions about it.",
    parameters: [] // Parameters are handled automatically
  },
  {
    name: "text_to_speech",
    description: "Converts your text responses to natural-sounding speech for the user to hear.",
    instructions: "This tool is automatically used for all your responses. Your text is converted to speech for the user to hear, so optimize your responses for listening rather than reading.",
    parameters: [] // Parameters are handled automatically
  },
  {
    name: "speech_to_text",
    description: "Converts the user's spoken words into text that you can understand and respond to.",
    instructions: "This tool is automatically used when the user speaks into their microphone. The transcribed text is provided to you, allowing you to understand what the user said verbally.",
    parameters: [] // Parameters are handled automatically
  },
  {
    name: "internet_search",
    description: "Searches the internet for information based on the user's query.",
    instructions: "Use this tool when the user asks a question that requires up-to-date information or information not readily available in your internal knowledge. Formulate a concise and effective search query based on the user's request.",
    parameters: [
      {
        name: "query",
        type: "string",
        description: "The search query to use.",
        required: true,
      },
    ],
  },
];

/**
 * Get the current date and time information
 * @returns {object} - Object containing date and time information
 */
export function getCurrentDateTime() {
  const now = new Date();

  // Format date components
  const date = now.toLocaleDateString('en-US', {
    weekday: 'long',
    year: 'numeric',
    month: 'long',
    day: 'numeric'
  });

  // Format time components
  const time = now.toLocaleTimeString('en-US', {
    hour: '2-digit',
    minute: '2-digit',
    second: '2-digit',
    hour12: true
  });

  return {
    full_datetime: `${date}, ${time}`,
    date: date,
    time: time,
    day_of_week: now.toLocaleDateString('en-US', { weekday: 'long' }),
    timezone: Intl.DateTimeFormat().resolvedOptions().timeZone,
    unix_timestamp: Math.floor(now.getTime() / 1000)
  };
}

/**
 * Perform an internet search
 * @param {object} args - Arguments for the search, including the query
 * @returns {Promise<object>} - The search results
 */
async function performInternetSearch(args) {
  const { query } = args;
  if (!query) {
    return { error: "Search query cannot be empty." };
  }

  try {
    // Replace with your actual internet search API endpoint and API key if required
    const searchApiUrl = `https://www.googleapis.com/customsearch/v1?key=YOUR_GOOGLE_CUSTOM_SEARCH_API_KEY&cx=YOUR_CSE_ID&q=${encodeURIComponent(query)}`;
    const response = await axios.get(searchApiUrl);
    if (response.data && response.data.items) {
      // Extract relevant information from the search results
      const results = response.data.items.map(item => ({
        title: item.title,
        link: item.link,
        snippet: item.snippet,
      }));
      return { results };
    } else {
      return { results: [], message: "No results found." };
    }
  } catch (error) {
    console.error("Error performing internet search:", error);
    return { error: `Failed to perform internet search: ${error.message}` };
  }
}

/**
 * Execute a tool based on its name and arguments
 * @param {string} toolName - The name of the tool to execute
 * @param {object} args - Arguments for the tool
 * @returns {Promise<object>} - The result of the tool execution
 */
export async function executeTool(toolName, args = {}) {
  switch (toolName) {
    case "wristwatch":
      return getCurrentDateTime();

    // For backward compatibility
    case "datetime":
      return getCurrentDateTime();

    case "internet_search":
      return performInternetSearch(args);

    // Other tools (image_analysis, text_to_speech, speech_to_text) are handled
    // automatically by the application flow, not directly called by the agent

    default:
      return { error: `Tool "${toolName}" not found or cannot be directly called.` };
  }
}

export default {
  tools,
  executeTool,
  getCurrentDateTime
};
Explanation of Changes:

internet_search Tool Definition:

A new tool object named "internet_search" is added to the tools array.
name: "internet_search" - This is the identifier your AI agent will use to request this tool.
description: "Searches the internet for information based on the user's query." - A clear description of what the tool does.
instructions: "Use this tool when the user asks a question that requires up-to-date information or information not readily available in your internal knowledge. Formulate a concise and effective search query based on the user's request." - Crucial instructions for when and how the AI agent should use this tool.
parameters: An array defining the expected input parameters for this tool.
name: "query" - The name of the parameter (the actual search term).
type: "string" - Specifies that the query should be a text string.
description: "The search query to use." - Describes what this parameter represents.
required: true - Indicates that the query parameter is mandatory for this tool.
performInternetSearch(args) Function:

This asynchronous function handles the actual internet search logic.
It takes an args object, which is expected to contain a query property.
Error Handling: It checks if the query is provided.
Internet Search API Call:
It uses the axios library (you need to install it: npm install axios) to make an HTTP GET request to a search API.
Important: Replace "YOUR_GOOGLE_CUSTOM_SEARCH_API_KEY" and "YOUR_CSE_ID" with your actual Google Custom Search Engine API key and Search Engine ID. You'll need to set up a Google Custom Search Engine to use this.
The encodeURIComponent(query) ensures that the search query is properly formatted for the URL.
Result Processing:
If the API call is successful and returns items, it maps the results to a simplified format including title, link, and snippet.
If no results are found, it returns an object indicating that.
Error Catching: It includes a try...catch block to handle potential errors during the API call.
executeTool Function Update:

A new case "internet_search": is added to the switch statement in the executeTool function.
When the toolName is "internet_search", it calls the performInternetSearch(args) function to execute the search.
How to Integrate with Your AI Agent:

When your AI agent determines that it needs to search the internet based on the user's query, it should:

Identify the internet_search tool: It should recognize that this tool is appropriate for the user's request.

Extract the search query: It needs to formulate a relevant search query from the user's input.

Call the executeTool function: Your agent needs to call the executeTool function with the name set to "internet_search" and the search query as an argument in the args object:

JavaScript

const query = "What is the current weather in Winter Haven, Florida?";
const searchResults = await executeTool("internet_search", { query: query });

if (searchResults.results) {
  console.log("Search Results:", searchResults.results);
  // Process the search results and formulate a response to the user
} else if (searchResults.error) {
  console.error("Internet search failed:", searchResults.error);
  // Handle the error appropriately
} else {
  console.log("No search results found.");
  // Inform the user that no relevant information was found
}
Before Running:

Install axios: If you haven't already, run npm install axios in your project directory.
Set up Google Custom Search Engine:
Go to https://cse.google.com/cse/all.
Create a new search engine. You can choose to search the entire web or specific sites.
Get your Search Engine ID (CX).
Go to the Google Cloud Console (https://console.cloud.google.com/).
Enable the Custom Search API.
Create API credentials and get your API Key.
Replace the placeholders in the performInternetSearch function with your actual API key and CSE ID.
This setup provides a basic internet search capability for your AI agent. You might want to enhance the performInternetSearch function to handle more sophisticated search parameters, result filtering, and error handling based on your specific needs.